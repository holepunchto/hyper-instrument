diff --git a/node_modules/prom-client/lib/metrics/heapSpacesSizeAndUsed.js b/node_modules/prom-client/lib/metrics/heapSpacesSizeAndUsed.js
index 9519a3b..a8caa50 100644
--- a/node_modules/prom-client/lib/metrics/heapSpacesSizeAndUsed.js
+++ b/node_modules/prom-client/lib/metrics/heapSpacesSizeAndUsed.js
@@ -1,7 +1,13 @@
 'use strict';
 
 const Gauge = require('../gauge');
-const v8 = require('v8');
+const v8 = {
+  getHeapSpaceStatistics: () => {
+    const err = new Error('Not implemented')
+    err.code = 'ERR_NOT_IMPLEMENTED'
+    throw err
+  }
+}
 
 const METRICS = ['total', 'used', 'available'];
 const NODEJS_HEAP_SIZE = {};
diff --git a/node_modules/prom-client/package.json b/node_modules/prom-client/package.json
index c9e41eb..8ffa474 100644
--- a/node_modules/prom-client/package.json
+++ b/node_modules/prom-client/package.json
@@ -11,6 +11,48 @@
 	"engines": {
 		"node": "^16 || ^18 || >=20"
 	},
+	"imports": {
+		"crypto": {
+			"bare": "bare-crypto",
+			"default": "crypto"
+		},
+		"events": {
+			"bare": "bare-events",
+			"default": "events"
+		},
+		"fs": {
+			"bare": "bare-fs",
+			"default": "fs"
+		},
+		"http": {
+			"bare": "bare-http1",
+			"default": "http"
+		},
+		"https": {
+			"bare": "bare-https",
+			"default": "https"
+		},
+		"os": {
+			"bare": "bare-os",
+			"default": "os"
+		},
+		"process": {
+			"bare": "bare-process",
+			"default": "process"
+		},
+		"url": {
+			"bare": "bare-url",
+			"default": "url"
+		},
+		"util": {
+			"bare": "bare-utils",
+			"default": "util"
+		},
+		"zlib": {
+			"bare": "bare-zlib",
+			"default": "zlib"
+		}
+	},
 	"scripts": {
 		"benchmarks": "node ./benchmarks/index.js",
 		"test": "npm run lint && npm run check-prettier && npm run compile-typescript && npm run test-unit",
